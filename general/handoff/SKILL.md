---
name: handoff
description: 将当前对话压缩为交接文档，供另一个智能体接手。
argument-hint: "下一个会话将用于什么？"
---

撰写一份交接文档，总结当前对话，以便新的智能体可以继续工作。将其保存到 `mktemp -t handoff-XXXXXX.md` 生成的路径（写入前先读取该文件）。

建议下一个会话应使用的技能（如有）。

不要重复其他产物（PRD、计划、ADR、工单、提交记录、差异）中已捕获的内容。改为通过路径或 URL 引用它们。

如果用户传入了参数，将其视为对下一个会话重点内容的描述，并据此调整文档。
